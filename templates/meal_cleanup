<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Schedule Import Meal Break Cleanup</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 font-sans">

<div class="max-w-5xl mx-auto p-6">
    <h1 class="text-3xl font-bold mb-6 text-center">Schedule Import Meal Break Cleanup</h1>

    <!-- Upload Section -->
    <div class="bg-white shadow rounded p-6 mb-6">
        <h2 class="text-xl font-semibold mb-4">Upload XML File</h2>
        <form action="/meal_cleanup/upload" method="post" enctype="multipart/form-data" class="flex items-center space-x-4">
            <input type="file" name="file" class="border rounded p-2 w-full" required>
            <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Upload</button>
        </form>
    </div>

    {% if xml_preview %}
    <!-- Cleanup Options -->
    <div class="bg-white shadow rounded p-6 mb-6">
        <h2 class="text-xl font-semibold mb-4">Cleanup Options</h2>
        <form action="/meal_cleanup/clean" method="post" class="space-y-4">
            <div>
                <label class="font-medium">Remove Empty Meal Breaks:</label>
                <input type="checkbox" name="remove_empty" class="ml-2">
            </div>

            <div>
                <label class="font-medium">Normalize Meal Break Times:</label>
                <input type="checkbox" name="normalize_times" class="ml-2">
            </div>

            <input type="hidden" name="xml_content" value="{{ xml_preview }}">
            <button type="submit" class="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700">Run Cleanup</button>
        </form>
    </div>
    {% endif %}

    {% if xml_preview %}
    <!-- XML Preview -->
    <div class="bg-white shadow rounded p-6 mb-6">
        <h2 class="text-xl font-semibold mb-4">XML Preview</h2>
        <textarea readonly class="w-full h-80 border rounded p-3 font-mono bg-gray-100 text-gray-800">{{ xml_preview }}</textarea>
    </div>

    <!-- Download -->
    <div class="bg-white shadow rounded p-6 text-center">
        <form action="/meal_cleanup/download" method="post">
            <input type="hidden" name="file_content" value="{{ xml_preview }}">
            <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 text-lg">Download Cleaned XML</button>
        </form>
    </div>
    {% endif %}
</div>

<footer class="mt-8 p-4 text-center text-gray-500 text-sm border-t border-gray-200">
    &copy; 2025 Nadi Jayasekara. All rights reserved. <br>
    Author of this App: <strong>Nadi Jayasekara</strong>
</footer>

</body>
</html>
